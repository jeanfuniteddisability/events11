// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCj7cvJoPUNW69dgQrcRz-fNIWtUdjWOxY",
    authDomain: "udc-events.firebaseapp.com",
    databaseURL: "https://udc-events.firebaseio.com",
    projectId: "udc-events",
    storageBucket: "udc-events.appspot.com",
    messagingSenderId: "94053683905",
    appId: "1:94053683905:web:1b126199cf8385c6f6fa40",
    measurementId: "G-BLZK86VQFJ"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
